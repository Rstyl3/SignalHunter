# PlatformIO build artifacts
.pio/
.pioenvs/
.piolibdeps/

# VS Code workspace files (contain absolute Windows user paths)
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch/

# Local clone of the upstream colleague's repo — keep for reference, do NOT
# push to our own remote (would duplicate the entire upstream codebase).
upstream/

# OS junk
.DS_Store
Thumbs.db

# Editor backups
*~
*.swp
*.bak
